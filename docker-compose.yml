---
version: '3.6'
services:
  xmas:
    image: "${STACK}:latest"
    ports:
      - "8043/tcp"
    deploy:
      labels:
        traefik.network: "public"
        traefik.enabled: 'true'
        traefik.port: 8043
        traefik.frontend.rules: 'Host: xmas2018.testnet.dapla.net'
      replicas: 1
    networks:
      - public

networks:
  public:
    external: true
    name: public
...
